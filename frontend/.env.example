NEXT_PUBLIC_API_URL=https://backends-httpnexus-dispatch-backend.cpcp6o.easypanel.host
NEXT_PUBLIC_WS_URL=https://backends-httpnexus-dispatch-backend.cpcp6o.easypanel.host
# Leave unset until the Grafana Public Dashboard link is activated —
# the header link only renders when this is present.
NEXT_PUBLIC_GRAFANA_URL=
