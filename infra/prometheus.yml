global:
  scrape_interval: 5s

scrape_configs:
  - job_name: nexus-backend
    static_configs:
      # host.docker.internal lets the Prometheus container reach your
      # backend running on the host via `npm run start:dev` (port 3000).
      - targets: ["host.docker.internal:3000"]
    metrics_path: /metrics
